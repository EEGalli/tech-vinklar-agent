# Pinnade versioner — exakta versioner skyddar mot att en kompromettad
# paketversion smyger sig in vid pip install. Dependabot bevakar
# uppdateringar och öppnar PR vid säkerhetsfixar.
requests==2.32.5
streamlit==1.54.0
google-generativeai==0.8.6
defusedxml==0.7.1
